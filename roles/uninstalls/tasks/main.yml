---
# - name: Uninstall all gems
#   shell: |
#     gem update --system
#     gem uninstall --all
#   become: true
#   become_user: root
- name: Uninstall existing Homebrew
  shell: "{{ item }}"
  with_items: "{{ uninstall_homebrew_steps }}"

- name: Remove existing Command Line Tools installation
  file:
    path: '{{ clt_path }}'
    state: absent
  become: yes